import type {AppBase, Entity} from 'playcanvas';

export function p13kFx(app: AppBase, system: string | number[], parent?: Entity): Entity[];
export function p13kPreload(app: AppBase, system: string | number[]): void;
